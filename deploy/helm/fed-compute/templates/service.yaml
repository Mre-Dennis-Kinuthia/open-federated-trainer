apiVersion: v1
kind: Service
metadata:
  name: {{ include "fed-compute.fullname" . }}
  labels:
    {{- include "fed-compute.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fed-compute.selectorLabels" . | nindent 4 }}
